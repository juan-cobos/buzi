[project]
name = "buzi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mne>=1.12.1",
]

[dependency-groups]
dev = [
    "pytest>=8",
    "ripple-detection>=1.7",
]
